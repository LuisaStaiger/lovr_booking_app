<%= form_with(model: [@festival, @love_pod], local: true) do |form| %>
  <% if @love_pod.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@love_pod.errors.count, "errors") %> prohibited this love pod from being saved:</h2>

      <ul>
        <% @love_pod.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :love_pod_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :love_pod_description %>
  </div>

  <div class="field">
    <%= form.label :capacity %>
    <%= form.number_field :capacity, id: :love_pod_capacity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>


